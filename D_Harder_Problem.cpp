#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int tc, n;
    cin >> tc;

    while (tc--){
        cin >> n;
        vector <int> v(n);
        for (int i = 0; i < n; i++)
            cin >> v[i];

        
    }

    return 0;
}
