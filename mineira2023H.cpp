#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;
int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);

    int n; string str;
    cin >> n; cin >> str;

    for (char i = 'a'; i <= 'z'; i++)
        freq[i] = 0;

    int l = 0, r = 1;

    cout << cnt + n << "\n";

    return 0;
}