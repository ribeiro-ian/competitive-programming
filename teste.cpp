#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;

bool solve(int change) {
    int bills[] = {2,5,10,20,50,100};
    
    // for (size_t i = 0; i < count; i++)
    // {
    //     /* code */
    // }
    
}

int main(){

    int n, m;

    while (1) {
        scanf("%i %i", &n,s &m);

        if (n == 0 and m == 0) break;

        int change = n - m;

        // printf("%s\n", solve(change) ? "possible" : "impossible");        
        printf("%i", n);
    }

    return 0;
}
