#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;

typedef struct {
    int x,y,z;
} Bloco;


int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);

    int n; cin >> n;
    vector <Bloco> v(n);
    for (auto &i : v) {
        cin >> i.x >> i.y >> i.z;
    }

    sort(v.begin(), v.end(), )

    return 0;
}