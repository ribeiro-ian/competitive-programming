#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define fastio ios::sync_with_stdio(0); cin.tie(0);

typedef struct {
  int x, y, z;
} Bloco;

int main() {
  fastio

  int n; cin >> n;
  vector<Bloco> v(n);
  for (auto &i : v) { cin >> i.x >> i.y >> i.z; }

  sort(v.begin(), v.end(), )

      return 0;
}