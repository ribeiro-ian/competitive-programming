#include <bits/stdc++.h>
using namespace std;
using ll = long long;

string k;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);

    cin >> k;
    int n; cin >> n;
    string str;
    while(n--){
        cin >> str;
    }

    return 0;
}