#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define fastio ios::sync_with_stdio(0); cin.tie(0);

string k;

int main() {
  fastio

  cin >> k;
  int n; cin >> n;
  string str;
  while (n--) { cin >> str; }

  return 0;
}